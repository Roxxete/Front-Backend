<?php
    
    //-------------------------------------------------------------------------------------------------------
    //          db()
    //-------------------------------------------------------------------------------------------------------
    $mysql = new mysqli(
        "localhost",
        "root",
        "",
        "bio"
    );

    if($mysql->connect_error)
    {
        die("Failed to conection" . $mysql->connect_error);
    }